# Product display name (single source of truth — read everywhere, never hardcode)
APP_NAME=BackTester
# Frontend mirror (set in frontend/.env.local once the UI exists — Slice 1)
NEXT_PUBLIC_APP_NAME=BackTester
# Database — added in Slice 3 (local Supabase / Postgres)
# DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:54322/postgres
